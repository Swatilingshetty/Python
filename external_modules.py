# external_modules:those modules which we download on our requirment

# pip: python package manager..
   #pip is the only way to download the modules in python..

# pip install package_name-- command to download the module...

# pip install pymongo

# list all the modules which are installed in your systems//
#   pip list or pip freeze

#  downloading the list of libraries from a text files//
   # pip install -p requirments.txt


# adding all the installed libraries into a text files..

# pip freeze > requirments.txt